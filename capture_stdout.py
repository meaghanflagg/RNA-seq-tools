#!/usr/bin/env python

import os, subprocess

#os.system("ls *.py")

#p=subprocess.Popen("ls *.py", stdout=subprocess.PIPE, stderr=subprocess.PIPE, shell=True)

#stdout, stderr = p.communicate()

#print stdout


# multiple commands:

#process=subprocess.Popen("test='mystring' ; echo $test", shell=True)

